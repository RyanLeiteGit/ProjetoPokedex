version: '3.8'

services:
  backend:
    build: ./login-backend
    ports:
      - "8000:8000"
    environment:
      
      - APP_USER=AlvoroVasco10
      - APP_PASSWORD=galomaiordeminas

  frontend:
    build: ./Pokedex
    ports:
      - "5173:5173"
    depends_on:
      - backend

    volumes:
      - ./Pokedex:/app
      - /app/node_modules
